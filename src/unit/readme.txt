这是词法分析器
使用的是职责链模式，不同的是，每个producer都能购同等的获得当前输入的字符，(这样就不需要回退了)
当然链中排在前面的可以率先输出（以准确度为准 如 123 应输出为字母，不应该是变量）
如发现该情况并不是自己处理得了，则主动退出，从此不再接受字符，到下一轮的处理（已经有输出之后）再加入（reset）
如果有其他特殊字符要加入（删除），则只需在职责链中相应位置加入（删除）即可

原因：词法分析无内嵌，也就是前面分析出来的东西跟后面无关，
相反，语法分析有内嵌，这也是麻烦所在。